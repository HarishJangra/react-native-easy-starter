import { DefaultTheme } from "react-native-paper";

const theme = {
	...DefaultTheme,
	dark: false,
	roundness: 2,
	colors: {
		...DefaultTheme.colors,
		background: "#fafafa"
	}
};

export default theme;

/**
 * react-native-elements colors
 * 		primary,
		secondary,
		grey0,
		grey1,
		grey2,
		grey3,
		grey4,
		grey5,
		greyOutline,
		searchBg,
		success,
		error,
		warning,
		divider
 */
