import { action, thunk } from "easy-peasy";
import { ApiService } from "../../Store";
import { STATUS, APP_STATE } from "../../Constants";

const checkAppVersion = thunk(
//api to check app version own server
)

const AppModel ={
 
}

export default AppModel